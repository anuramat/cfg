exec_always {
    # # perf hogs!!!
    # pkill inactive-windo; inactive-windows-transparency.py
    # pkill mpvpaper; mpvpaper '*' ~/Videos/waves.mp4 -fo "loop-file=inf"
    "pkill swayidle; swayidle -w"
    # network manager tray thingie
    "pkill nm-applet; nm-applet"
    # blueman tray thing
    "pkill blueman-tray; blueman-tray"
    # avizo: brightness/volume overlay notifications
    "pkill avizo-service; avizo-service"
    way-displays
    # way-displays doesn't need to be killed, breaks shit otherwise
    "pkill udiskie; udiskie -ta"
}

# vim: ft=swayconfig
